define( [ "jquery", "underscore", "backbone" ], function( $, _, Backbone ) {
	var {{{viewName}}} = Backbone.View.extend({
		initialize: function() {
			// Initialize model
		}
	});
	
	return {{{viewName}}};
});