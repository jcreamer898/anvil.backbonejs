define( [ "underscore", "backbone" ], function( _, Backbone )  {
	var {{{routerName}}} = Backbone.Router.extend({
		initialize: function() {
			// Initialize Router.
		}
	});

	return {{{modelName}}};
});